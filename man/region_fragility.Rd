% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fragility.R
\name{region_fragility}
\alias{region_fragility}
\title{Regional fragility summary for a CausalSpline fit}
\usage{
region_fragility(
  object,
  a,
  b,
  grid = NULL,
  h = NULL,
  eps = NULL,
  type = c("curvature_ratio", "inverse_slope"),
  normalize = TRUE,
  t_obs = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted \code{causal_spline} object.}

\item{a}{Numeric scalar. Lower bound of the integration interval.}

\item{b}{Numeric scalar. Upper bound of the integration interval.}

\item{grid}{Optional numeric evaluation grid within \eqn{[a, b]}. If
\code{NULL}, a dense grid of 300 points is created automatically.}

\item{h}{Step size for finite differences. Default \code{NULL} (auto).}

\item{eps}{Adaptive eps passed to \code{\link{fragility_curve}}.
Default \code{NULL} (auto).}

\item{type}{Fragility definition: \code{"curvature_ratio"} (default) or
\code{"inverse_slope"}.}

\item{normalize}{Logical. Divide integral by interval length? Default
\code{TRUE}.}

\item{t_obs}{Optional numeric vector of observed treatment values for
support density. Default \code{NULL}.}

\item{...}{Ignored.}
}
\value{
A list with elements:
\describe{
  \item{\code{interval}}{Named numeric vector \code{c(a, b)} after clamping
    to the observed support.}
  \item{\code{type}}{Fragility type used.}
  \item{\code{integral_fragility}}{Trapezoidal integral of fragility over
    \eqn{[a, b]}.}
  \item{\code{average_fragility}}{Integral divided by interval length.}
  \item{\code{normalized}}{Logical flag.}
  \item{\code{curve}}{The full \code{fragility_curve} data frame.}
}
}
\description{
Integrates the pointwise fragility curve over a treatment interval
\eqn{[a, b]} using the trapezoidal rule. Useful for comparing sensitivity
across dose ranges (e.g., low vs. high dose) or summarising instability
at a policy-relevant threshold.
}
\examples{
dat <- simulate_dose_response(200, dgp = "threshold")
fit <- causal_spline(Y ~ T | X1 + X2 + X3, data = dat)
region_fragility(fit, a = 2, b = 5)

}
