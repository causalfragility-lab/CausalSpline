% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.causal_spline}
\alias{predict.causal_spline}
\title{Predict E[Y(t)] at new treatment values}
\usage{
\method{predict}{causal_spline}(object, newt, se_fit = FALSE, warn_extrap = TRUE, ...)
}
\arguments{
\item{object}{A \code{causal_spline} object.}

\item{newt}{Numeric vector of treatment values at which to predict.}

\item{se_fit}{Logical. Return standard errors? Default \code{FALSE}.}

\item{warn_extrap}{Logical. Warn if any values in \code{newt} fall outside
the observed treatment range? Default \code{TRUE}.}

\item{...}{Ignored.}
}
\value{
A data frame with columns \code{t}, \code{estimate},
  \code{extrapolated}, and optionally \code{se}, \code{lower}, \code{upper}.
  The \code{extrapolated} column is \code{TRUE} for any \code{newt} value
  outside the observed treatment support.
}
\description{
Predict E[Y(t)] at new treatment values
}
\examples{
dat <- simulate_dose_response(200)
fit <- causal_spline(Y ~ T | X1 + X2 + X3, data = dat)
predict(fit, newt = c(1, 2, 3, 4, 5))

}
