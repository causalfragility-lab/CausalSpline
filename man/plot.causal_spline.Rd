% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.causal_spline}
\alias{plot.causal_spline}
\title{Plot the estimated dose-response curve}
\usage{
\method{plot}{causal_spline}(
  x,
  rug = TRUE,
  truth = NULL,
  xlab = "Treatment (T)",
  ylab = "E[Y(t)]",
  title = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{causal_spline} object.}

\item{rug}{Logical. Add a treatment distribution rug. Default \code{TRUE}.}

\item{truth}{Optional data frame with columns \code{t} and \code{true_effect}
for overlaying the true dose-response (useful in simulations).}

\item{xlab}{Character. x-axis label. Default \code{"Treatment (T)"}.}

\item{ylab}{Character. y-axis label. Default \code{"E[Y(t)]"}.}

\item{title}{Character. Plot title. Default \code{NULL}.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plots the estimated causal dose-response curve \eqn{E[Y(t)]} against \eqn{t}
with pointwise confidence bands and an optional rug for the observed
treatment distribution.
}
\examples{
dat <- simulate_dose_response(500, dgp = "threshold")
fit <- causal_spline(Y ~ T | X1 + X2 + X3, data = dat)
plot(fit)

}
